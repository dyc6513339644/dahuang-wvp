<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.wvp.mapper.RecordPlanMapper">

    <select id="queryRecordIng" resultType="int">
        SELECT wdc.id
        FROM wvp_device_channel wdc
        LEFT JOIN wvp_record_plan_item wrpi ON wrpi.plan_id = wdc.record_plan_id
        WHERE wrpi.week_day = #{week}
          AND wrpi.start &lt;= #{index}
          AND wrpi.stop &gt;= #{index}
        GROUP BY wdc.id
    </select>

</mapper>

# 快速上手：dahuang-wvp 国标视频管理平台

## 一、项目简介

**dahuang-wvp** 是基于 [若依 (RuoYi-Vue)](http://www.ruoyi.vip) 和 [WVP](https://github.com/648540858/wvp-GB28181-pro) 的 GB/T 28181-2016 国标视频管理平台。通过国标 28181 协议将摄像头、NVR 等设备接入平台，实现视频流的在线预览、回放和分发。

### 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | Spring Boot 2.7.18 |
| 安全框架 | Spring Security + JWT |
| ORM | MyBatis + PageHelper |
| 数据库 | MySQL (Druid 连接池) |
| 缓存 | Redis (Lettuce) |
| 消息协议 | SIP (GB/T 28181) |
| 流媒体服务 | ZLMediaKit |
| 构建工具 | Maven 3.1+ |
| Java 版本 | JDK 1.8 |

---

## 二、环境准备

### 2.1 必需组件

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| JDK | 1.8+ | 推荐 OpenJDK 8 |
| Maven | 3.1+ | 项目用 Maven 管理依赖 |
| MySQL | 5.7+ | 存储业务数据 |
| Redis | 5.0+ | 缓存设备状态、会话信息 |
| ZLMediaKit | 最新版 | 流媒体服务，处理视频流转发 |

### 2.2 安装 MySQL

1. 确保 MySQL 服务运行在 `127.0.0.1:3307`（可根据实际情况修改配置）
2. 创建数据库：

```sql
CREATE DATABASE IF NOT EXISTS `dahuang-wvp` DEFAULT CHARACTER SET utf8mb4;
```

3. 导入项目附带的 SQL 初始化脚本（如有），确保包含 `wvp_device`、`wvp_sip_config`、`sys_config` 等核心表。

### 2.3 安装 Redis

1. 确保 Redis 运行在 `127.0.0.1:6379`（可根据实际情况修改配置）
2. 默认使用 `database 2`，无需密码

### 2.4 安装 ZLMediaKit（流媒体服务）

> ZLMediaKit 是视频流转发的核心，没有它视频流无法正常播放。

1. 从 [ZLMediaKit GitHub](https://github.com/ZLMediaKit/ZLMediaKit) 获取并编译安装
2. 配置 ZLMediaKit 的 `config.ini`：

```ini
[api]
apiDebug=1
secret=zAH8Muz7KMz3XzkbQXPKJSnGOWKrjJ73

[http]
port=8089

[rtp]
portRange=40000-40300

[hook]
enable=1
admin_params=secret=zAH8Muz7KMz3XzkbQXPKJSnGOWKrjJ73
```

3. 启动 ZLMediaKit，确认 `http://127.0.0.1:8089` 可访问

---

## 三、配置文件说明

项目有两个主要配置文件，位于 `ruoyi-admin/src/main/resources/`：

### 3.1 application.yml — 主配置文件

```yaml
server:
  port: 8080                          # HTTP 端口

spring:
  redis:
    host: 127.0.0.1                   # Redis 地址，按实际修改
    port: 6379                        # Redis 端口
    database: 2

sip:
  ip:                                 # SIP 服务 IP，留空则自动检测网卡
  port: 8116                          # SIP 信令监听端口
  domain: 4101050000
  id: 41010500002000000001            # 本平台在国标中的唯一ID
  password: 12345678                  # 设备接入认证密码

media:
  id: FQ3TF8yT83wh5Wvz                # ZLMediaKit 的 mediaServerId
  ip: 127.0.0.1                       # ZLM 服务地址，按实际修改
  http-port: 8089                     # ZLM HTTP 端口，按实际修改
  secret: zAH8Muz7KMz3XzkbQXPKJSnGOWKrjJ73  # 与 ZLM 配置一致

user-settings:
  play-timeout: 180000                # 点播超时时间(毫秒)，默认3分钟
```

### 3.2 application-druid.yml — 数据库配置

```yaml
spring:
  datasource:
    druid:
      master:
        url: jdbc:mysql://127.0.0.1:3307/dahuang-wvp?...   # 修改为你的数据库地址
        username: root                                       # 修改为你的用户名
        password: root123456                                 # 修改为你的密码
```

### 3.3 首次运行必须修改的配置项

| 配置项 | 文件 | 说明 |
|--------|------|------|
| `spring.datasource.druid.master.url` | application-druid.yml | 数据库连接地址 |
| `spring.datasource.druid.master.username` | application-druid.yml | 数据库用户名 |
| `spring.datasource.druid.master.password` | application-druid.yml | 数据库密码 |
| `spring.redis.host` | application.yml | Redis 地址 |
| `media.ip` | application.yml | ZLMediaKit 地址 |
| `media.http-port` | application.yml | ZLMediaKit HTTP 端口 |
| `sip.ip` | application.yml | 本机 SIP 监听 IP |

---

## 四、项目结构

```
dahuang-wvp-serve/
├── ruoyi-admin/          # 启动模块 + 系统管理 Controller
│   └── src/main/resources/
│       ├── application.yml          # 主配置
│       ├── application-druid.yml    # 数据源配置
│       └── civilCode.csv           # 行政区划数据
├── ruoyi-wvp/            # WVP 核心模块（国标28181功能）
│   └── src/main/java/com/ruoyi/wvp/
│       ├── controller/     # API 接口层（设备、点播、云台等）
│       ├── gb28181/        # 国标 28181 协议实现
│       │   ├── bean/       # 实体类（Device、DeviceChannel 等）
│       │   ├── service/    # 核心业务服务（PlayServiceImpl 等）
│       │   ├── session/    # SIP 会话管理、SSRC 分配
│       │   ├── transmit/   # SIP 消息发送与接收
│       │   └── event/      # SIP 事件处理
│       ├── media/          # 流媒体服务对接
│       ├── service/        # RTP 服务器管理
│       └── conf/           # 配置类（UserSetting、SipConfig 等）
├── ruoyi-framework/       # 框架层（安全、数据源、AOP）
├── ruoyi-system/          # 系统管理（用户、角色、菜单）
├── ruoyi-common/          # 通用工具（Redis、异常、工具类）
├── ruoyi-quartz/          # 定时任务模块
└── ruoyi-generator/       # 代码生成器模块
```

### 核心包说明

- **ruoyi-wvp** — 最核心的模块，包含国标 28181 协议的完整实现
  - `PlayController` — 视频点播 API（`/api/play/*`）
  - `PlayServiceImpl` — 点播核心逻辑（RTP 收流、SIP INVITE、流状态管理）
  - `SIPCommander` — SIP 命令发送（playStreamCmd、playbackStreamCmd）
  - `SIPRequestHeaderProvider` — SIP 请求消息构造
  - `SIPSender` — SIP 消息底层发送
  - `SipLayer` — SIP 监听服务管理
  - `RtpServerServiceImpl` — RTP 收流端口管理

---

## 五、启动项目

### 5.1 命令行启动

```powershell
# 1. 编译打包（跳过测试）
mvn clean package -DskipTests -pl ruoyi-admin -am

# 2. 启动
java -jar ruoyi-admin\target\ruoyi-admin.jar --spring.profiles.active=druid
```

### 5.2 IDE 启动

直接用 IntelliJ IDEA / Eclipse 打开项目，运行 `com.ruoyi.RuoYiApplication` 的 `main` 方法。

### 5.3 启动成功标志

日志中出现以下内容表示启动成功：

```
(♥◠‿◠)ﾉﾞ  ruoyi-wvp启动成功   ლ(´ڡ`ლ)ﾞ
```

### 5.4 访问系统

- **前端页面**：`http://127.0.0.1:8080`
- **默认账号**：`admin` / `admin123`
- **Swagger 文档**：`http://127.0.0.1:8080/swagger-ui/index.html`
- **Druid 监控**：`http://127.0.0.1:8080/druid`（账号：`ruoyi` / `123456`）

---

## 六、核心 API 概览

| Controller | 路径前缀 | 功能 |
|------------|---------|------|
| PlayController | `/api/play` | 视频点播（开始、停止） |
| PlaybackController | `/api/playback` | 录像回放 |
| DeviceQueryController | `/api/device/query` | 设备/通道查询、同步 |
| DeviceControlController | `/api/device/control` | 云台控制 |
| PlatformController | `/api/platform` | 国标级联（平台对接） |
| StreamPushController | `/api/push` | 推流管理 |
| StreamProxyController | `/api/proxy` | 拉流代理 |
| GBRecordController | `/api/gb_record` | 国标录像查询 |
| MobilePositionController | `/api/position` | 设备位置/轨迹 |
| CloudRecordController | `/api/cloud/record` | 云录制管理 |
| RtpController | `/api/rtp` | RTP 收流/发流 |
| GroupController | `/api/group` | 分组管理 |
| AlarmController | `/api/alarm` | 报警管理 |
| SipServerController | `/api/sip/server` | SIP 信令服务管理 |

### 关键接口示例

```
# 视频点播（核心接口）
GET /api/play/start/{deviceId}/{channelId}

# 停止点播
GET /api/play/stop/{deviceId}/{channelId}

# 设备列表
GET /api/device/query/devices

# 通道列表
GET /api/device/query/devices/channels

# 录像回放
GET /api/playback/start?deviceId=xxx&channelId=xxx&startTime=xxx&endTime=xxx
```

---

## 七、点播流程详解

了解这个流程有助于排查问题：

```
HTTP请求(play/start)
  │
  ▼
1. 查询设备信息（Redis/MySQL）
2. 查询通道信息
3. 获取可用的 ZLMediaKit 节点
4. 开放 RTP 收流端口（调用 ZLM API）
5. 分配 SSRC
  │
  ▼
6. 发送 SIP INVITE 给设备
  │
  ├─ 成功 ──► 设备返回 200 OK
  │               │
  │               ▼
  │          设备推送 RTP 流到 ZLM
  │               │
  │               ▼
  │          ZLM 回调 on_media_arrival
  │               │
  │               ▼
  │          返回播放地址给前端
  │
  └─ 失败 ──► 清理 SSRC + 端口
              返回错误（-10 命令发送失败 等）
```

---

## 八、常见问题排查

### 8.1 启动报 Redis 连接失败

```
Caused by: io.netty.channel.AbstractChannel$AnnotatedConnectException: Connection refused
```

**解决**：确认 Redis 已启动，检查 `application.yml` 中 `spring.redis.host` 和 `port` 是否正确。

### 8.2 启动报 MySQL 连接失败

```
Caused by: java.net.ConnectException: Connection refused
```

**解决**：确认 MySQL 已启动，检查 `application-druid.yml` 中的连接地址、用户名和密码。

### 8.3 启动后 ZLM 连接失败

```
[ZLM-尝试连接]失败, ID：xxx, 地址：192.168.99.222:8089
```

**解决**：
1. 确认 ZLMediaKit 已启动
2. 检查 `application.yml` 中 `media.ip` 和 `media.http-port` 是否正确
3. ZLM 的 `secret` 需与配置文件一致

### 8.4 点播接口返回 `{"code":-10,"msg":"命令发送失败"}`

这个错误表示 SIP INVITE 请求构造或发送失败。常见原因：

1. **设备未完成注册** — 设备的 `hostAddress` 或 `transport` 字段为空
2. **Redis 缓存丢失** — 设备信息缓存在 Redis 中过期或被清空
3. **SIP 服务未启动** — 网卡 IP 未正确绑定

**排查步骤**：
1. 搜索日志关键词 `[命令发送失败] 点播消息`，查看具体异常类型
2. 检查设备是否在线（`/api/device/query/devices`）
3. 等待设备重新注册，或手动补全设备的 `host_address` 和 `transport` 字段

### 8.5 点播超时

日志中出现 `[点播等待超时]`

**可能原因**：
1. 设备不在线或网络不通
2. 设备正在被其他人点播（SSRC 冲突）
3. 防火墙拦截了 RTP 端口（40000-40300）
4. TCP 模式下 SSRC 校验导致收流失败

**解决**：
- 增加超时时间：`user-settings.play-timeout: 180000`
- 使用 UDP 模式代替 TCP
- 关闭 SSRC 校验：`ssrcCheck: false`

---

## 九、分支管理

| 分支 | 说明 |
|------|------|
| `master` | 主分支 |
| `dev1.1.0` | 1.1.0 开发分支 |
| `dev-1.1.1` | 1.1.1 开发分支（当前） |

```powershell
# 查看所有分支
git branch -a

# 切换分支
git checkout -b dev-1.1.1 origin/dev-1.1.1

# 暂存当前修改后切换
git stash
git checkout dev-1.1.1
git stash pop   # 恢复暂存的修改
```

---

## 十、快速启动检查清单

在首次启动前，逐项确认以下内容：

- [ ] JDK 1.8 已安装
- [ ] Maven 3.1+ 已安装
- [ ] MySQL 已启动，数据库 `dahuang-wvp` 已创建
- [ ] Redis 已启动
- [ ] ZLMediaKit 已安装并启动
- [ ] `application-druid.yml` 中数据库连接信息正确
- [ ] `application.yml` 中 Redis 地址正确
- [ ] `application.yml` 中 ZLM 地址和端口正确
- [ ] `application.yml` 中 SIP 配置与 ZLM 一致
- [ ] 已执行数据库初始化脚本

全部确认后，运行 `mvn clean package -DskipTests -pl ruoyi-admin -am` 编译，然后 `java -jar ruoyi-admin\target\ruoyi-admin.jar --spring.profiles.active=druid` 启动即可。
